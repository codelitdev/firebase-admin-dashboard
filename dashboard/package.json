{
  "name": "webapp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "3",
    "firebase": "^5.0.4",
    "react": "^16.4.0",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "REACT_APP_ENV=local react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "buildncopy": "yarn run build && cd ../ && rm -rf public && cp -R ./dashboard/build . && mv build public"
  },
  "devDependencies": {
    "jsdom": "^11.11.0",
    "react-test-renderer": "^16.4.0"
  }
}
